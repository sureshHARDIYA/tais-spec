{
  "name": "acme-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/cli": "2.4.0"
  },
  "private": true,
  "scripts": {
    "start": "redocly preview-docs",
    "build": "redocly build-docs openapi/openapi.yaml --output=dist && cp dist docs/index.html",
    "build:docs": "redocly build-docs --output=site",
    "test": "redocly lint"
  }
}
